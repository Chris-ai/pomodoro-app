import { SettingsIcon } from "../icons/settings";

export const SettingsButton = () => {
  return (
    <button className="bg-transparent cursor-pointer grid place-items-center">
      <SettingsIcon />
    </button>
  );
};
